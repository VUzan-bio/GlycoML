# Models

Model checkpoints are written here by training scripts but are not versioned.
Only lightweight Python modules under `models/` are tracked.

Writers:
- `scripts/train/train_phase2_with_glycan_encoder_export.py`
- `scripts/train/train_fcgr_binding_model.py`
- `scripts/train/train_fcgr_with_transfer_learning.py`
